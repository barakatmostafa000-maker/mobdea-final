# مراجعة مشروع منصة المُبدع قبل التطوير

**النسخة التي تمت مراجعتها:** 9.2.0
**نطاق المراجعة:** جميع مجلدات `src` و`public` و`cloud-worker` و`android` والاختبارات وملفات البناء.
**قاعدة التنفيذ:** استكمال الموجود وعدم استبدال أي جزء يعمل، مع إبقاء نموذج البيانات الحالي قابلًا للترحيل.

## ملخص معماري

- الواجهة: React 19 + Vite، مع صفحات Lazy-loaded داخل `App.jsx`.
- Android: Capacitor 7.
- التخزين: سجل JSON مشفر محليًا، مع ملفات كبيرة مشفرة منفصلة في IndexedDB/Asset Store.
- السحابة: Cloudflare Worker + KV/R2. لا توجد حاليًا حزمة أو عميل Supabase داخل المشروع.
- نمط البيانات: كائن تطبيق مركزي يمر إلى الصفحات عبر `data` و`updateData`، مع تطبيع وترحيل داخل `services/storage.js`.
- المحتوى: قائمة مسطحة `contentLibrary`؛ المورد الواحد قد يحمل الصف والوحدة والدرس والصفحات والوسائط والأسئلة.

## نتيجة مراجعة الوحدات المطلوبة

### 1. وضع الحصة — منفذ جزئيًا وقابل للتمديد

**الموجود ويعمل:**
- اختيار المورد حسب صف الحصة الحالية.
- PDF وصور وفيديو وصوت وسبورة وخرائط.
- رسم وكتابة وأشكال فوق PDF والصور، مع طبقة محفوظة لكل مورد/صفحة.
- حضور، نقاط، تشجيع، اختيار عشوائي، تسجيل الشاشة، رابط الطالب، وحفظ ملخص الحصة.
- خطة حصة من موارد مثبتة في المكتبة.

**الناقص:**
- اختيار الدرس تلقائيًا وتحميل كل وسائطه كمجموعة واحدة.
- استخدام كتاب الصف الرئيسي تلقائيًا مع نطاق صفحات الدرس.
- خريطة تلقائية حسب الصف داخل وضع الحصة.
- أدوات جغرافية تعليمية جانبية مخصصة للشرح، وحفظ حالة الخريطة داخل الدرس.

**لا يجب تغييره:** نظام الحضور والنقاط والتسجيل والرسم الحالي، ومفاتيح `classResourceQueue` القديمة لضمان التوافق.

### 2. المكتبة — منفذة جزئيًا

**الموجود ويعمل:**
- رفع PDF وصور وفيديو وصوت ومستندات وروابط وحفظها مشفرة.
- فلترة حسب الصف والوحدة والدرس والنوع.
- معاينة الموارد، تثبيتها في خطة الحصة، وربط أسئلة مولدة بها.
- إمكانية إرفاق ملف امتحانات داخل مورد من نوع كتاب.

**الناقص مقارنة بالطلب:**
- لا يوجد فصل واضح بين “كتاب الصف الرئيسي” و“ملف امتحانات الصف الرئيسي”.
- لا يوجد كيان Lesson مستقل يجمع التاريخ والصفحات والوسائط والتسجيل والصورة المصغرة.
- الواجهة قائمة موارد مسطحة وليست صفوفًا قابلة للتوسيع ودروسًا بداخلها.
- وضع الحصة لا يفتح درسًا كاملًا تلقائيًا.

**ما يجب الحفاظ عليه:** كل الموارد الحالية ومعرفاتها وملفاتها وأسئلتها؛ يجب ترحيلها منطقيًا دون حذفها.

### 3. السبورة — منفذة جيدًا

- صفحات متعددة، رسم، نصوص، أشكال، تحديد، تحريك، تراجع وإعادة.
- فتح PDF وصور والكتابة عليها.
- حفظ سجلات السبورة وربطها بالمنصة.

**تحسين آمن:** إعادة استخدام أدوات الرسم نفسها فوق خريطة وضع الحصة بدل إنشاء محرر رسم منفصل.

### 4. PDF Viewer — موجود كخدمة وليس كوحدة مستقلة

- `usePdfPage` و`pdfRenderer` يعرضان صفحة PDF كرسم قابل للكتابة فوقه.
- توجد عودة إلى iframe عند تعذر الرسم الأصلي.

**الناقص:** فرض نطاق صفحات الدرس ومنع التنقل خارج الصفحات المختارة.

### 5. الصور والفيديو والصوت — موجودة داخل المكتبة ووضع الحصة

- الملفات تحفظ منفصلة ومشفرة.
- الصور قابلة للرسم فوقها، والفيديو والصوت قابلان للتشغيل.

**الناقص:** تجميع عدة وسائط في درس واحد والتنقل بينها تلقائيًا.

### 6. الخرائط التفاعلية — تحدي الخرائط منفذ جيدًا، خرائط الحصة ناقصة

**تحدي الخرائط الموجود:**
- GeoJSON محلي دون إنترنت.
- أوضاع تحدي وتدريب واستكشاف وبناء وبطولات.
- دول وتضاريس ومياه وثروات وعواصم، رسم وتكبير وتسميات.

**المشكلة:** وضع الحصة يضم صفحة `MapChallenge` كاملة داخل اللوحة، بدل خريطة شرح مهيأة للصف والدرس.

**التمديد الآمن:** استخراج بيانات الخريطة ومكون العرض إلى طبقة مشتركة، ثم بناء “استوديو خريطة الحصة” عليها، مع إبقاء تحدي الخرائط كما هو.

### 7. لوحة التحكم — منفذة

- بحث، جدول، حصة حالية، إعلانات، امتحانات وبطاقات انتقال حقيقية.
- لا تحتاج إعادة تصميم في هذا النطاق.

### 8. نظام الطلاب — منفذ

- CRUD، أكواد وPIN، صلاحيات، حضور ودرجات ومدفوعات وكروت.
- يجب عدم تغيير نموذج الطالب ضمن هذا التطوير.

### 9. بنك الأسئلة والامتحانات — منفذ جزئيًا

- بنك أساسي + مخصص، استيراد، إنشاء امتحانات، رصد وتحليل.
- يولد أسئلة من بيانات مورد المكتبة النصية.

**الناقص:** تعريف ملف امتحانات رئيسي مستقل لكل صف وإظهاره كمصدر رسمي لبنك الأسئلة/المولد.
- لا توجد قراءة نص PDF أو OCR داخل المشروع؛ لذلك يمكن ربط الملف كمصدر دائم ومرجع، لكن استخراج أسئلة منه تلقائيًا يحتاج مزود Parsing/AI لاحقًا.

### 10. النسخ الاحتياطي والمزامنة السحابية — منفذان

- نسخة احتياطية مشفرة مع ملفات اختيارية وكشف العبث.
- مزامنة Cloudflare مع عزل مساحة العمل ورفع ملفات منفصل.
- يجب توسيع جمع Asset IDs ليشمل حقول الكتاب/الامتحانات/وسائط الدروس الجديدة.

### 11. Supabase Integration — غير موجود

- لا توجد حزمة `@supabase/supabase-js` ولا إعدادات أو خدمات Supabase.
- المشروع يستخدم Cloudflare Worker بدلًا منه.
- لن تتم إضافة Supabase دون بيانات مشروع ومفاتيح وسياسة واضحة؛ إضافة تكامل ثانٍ الآن ستكرر المزامنة وتخلق تعارضًا.

### 12. المساعد الذكي والتقارير

- “مساعد المُبدع” حاليًا محرك Insights محلي وليس نموذج AI خارجي.
- التقارير تعتمد على الطلاب والحضور والدرجات والمدفوعات.

**تمديد آمن:** توفير Selectors موحدة للدروس وملفات الصف، كي تعتمد عليها الوحدات الحالية دون إدخال مزود AI وهمي.

## المشكلات العامة التي ظهرت في المراجعة

1. `ContentLibrary.jsx` و`ClassMode.jsx` كبيران ويجمعان منطقًا وواجهة؛ سيتم إضافة خدمات/مكونات مشتركة بدل زيادة التكرار.
2. بيانات المكتبة مسطحة؛ يلزم Schema متوافق يميز: `gradeTextbook` و`gradeExams` و`lesson` و`lessonMedia` مع الحفاظ على الموارد القديمة.
3. بيانات الخريطة موجودة داخل صفحة التحدي؛ يلزم نقلها إلى Module مشترك.
4. بيانات Seed تحتوي روابط `#` تجريبية؛ يجب ألا تعتبر ملفات صالحة.
5. لا توجد اختبارات لمحددات الدروس/الصفوف أو التعيين التلقائي للخريطة؛ يجب إضافتها.

## قرار التنفيذ بعد المراجعة

- **لن يعاد بناء أي وحدة من الصفر.**
- سيتم إنشاء طبقة `libraryModel` لتطبيع الموارد القديمة وتوفير مصدر موحد للكتاب والامتحانات والدروس والوسائط.
- سيتم تحديث المكتبة لتعرض بطاقتي الصف الثابتتين ثم أقسام الصفوف والدروس.
- سيتم تحديث وضع الحصة لفتح الدرس وكتاب الصف ونطاق صفحاته ووسائطه تلقائيًا.
- سيتم إنشاء مكون خريطة حصة مشترك يعتمد على نفس GeoJSON والبيانات المستخدمة في تحدي الخرائط.
- سيتم تحديث النسخ الاحتياطي والمزامنة والتطبيع والاختبارات لتشمل الحقول الجديدة.


## نتيجة التنفيذ بعد المراجعة — الإصدار 9.3.0

تم تنفيذ القرار المذكور أعلاه دون إعادة بناء المشروع من الصفر:

- أضيفت طبقة `libraryModel` مع ترحيل تلقائي للموارد القديمة.
- أصبحت للمكتبة بطاقتا كتاب وامتحانات دائمتان لكل صف، ودروس قابلة للتوسيع بوسائط دائمة.
- أصبح وضع الحصة يفتح الدرس وكتاب الصف ونطاق الصفحات والوسائط تلقائيًا.
- أضيف استوديو خريطة شرح مشترك بخريطة تلقائية حسب الصف وشريط ظواهر جغرافية شامل.
- أضيف حفظ مستقل لكل خريطة ومنطقة، وربط الواجبات والتقارير وبنك الأسئلة وملف الامتحانات بالمكتبة.
- تم تحديث التخزين والمزامنة والنسخ الاحتياطي لجمع الأصول الجديدة.
- اجتازت النسخة 24 اختبارًا آليًا وجميع الفحوص الساكنة المتاحة.

التفاصيل الكاملة في `LIBRARY_MAPS_IMPLEMENTATION_AR.md`.
