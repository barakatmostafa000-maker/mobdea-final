# نشر تطبيق الطلاب مرة واحدة والتحديث التلقائي

## النتيجة

تطبيق الطلاب يُنشر كـ PWA على GitHub Pages. يرسل المعلم الرابط للطلاب مرة واحدة، ويستطيع الطالب تثبيته من المتصفح على الشاشة الرئيسية. كل Push إلى فرع `main` يشغّل الفحص والبناء والنشر تلقائيًا.

## الإعداد مرة واحدة على GitHub

1. افتح المستودع ثم `Settings`.
2. افتح `Pages`.
3. في `Build and deployment` اختر `GitHub Actions`.
4. ارفع هذه النسخة إلى فرع `main`.
5. انتظر نجاح Workflow باسم `Deploy Mobdea Student PWA`.
6. انسخ رابط GitHub Pages وأرسله للطلاب.

## طريقة التحديث عند الطلاب

- ملفات JavaScript وCSS تُحمّل بأسلوب Network First لتجنب الشاشة البيضاء الناتجة عن خلط ملفات إصدارين.
- Service Worker يفحص إصدارًا جديدًا كل 15 دقيقة وعند إعادة فتح التطبيق.
- عندما يصبح إصدار جديد جاهزًا تظهر رسالة تحديث، ثم يعاد فتح التطبيق على الإصدار الجديد دون إعادة إرسال APK.
- البيانات المحلية للطالب لا تُحذف أثناء تحديث الواجهة.

## تطبيق Android للمعلم

تطبيق Android يبقى له مسار تحديث APK آمن من صفحة التحديثات. لا يمكن تثبيت APK صامتًا من مستودع عادي بسبب قيود Android؛ المستخدم يوافق على التثبيت، أو يتم النشر على Google Play لاستخدام تحديثات المتجر.
